[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Route all requests to Nitro server function for SSR
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200