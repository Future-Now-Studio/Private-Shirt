[build]
  command = "npm run generate"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# All routes go to index.html for SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200